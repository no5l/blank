name: Workflow 2
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Get code
        uses: actions/checkout@v3
      - name: Install python
      - uses: actions/setup-python@v4
        with:
          python-version: '3.10' 
